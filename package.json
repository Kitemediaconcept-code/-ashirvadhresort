{
  "name": "ashirvadhresort",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd resort-web && npm install && npm run build",
    "dev": "cd resort-web && npm run dev"
  },
  "engines": {
    "node": ">=22"
  }
}
